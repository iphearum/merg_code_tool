var current_no_up;
var elm_container3 = ajax_var_upload_3.plupload.container;
var elm_uploaded_images3 = ajax_var_upload_3.upload_imagelist;
var imagelist3 = ajax_var_upload_3.imagelist;
var btn_id3 = ajax_var_upload_3.plupload.browse_button;

jQuery(document).ready(function(c) {
    delete_binder(elm_container3, imagelist3);
    var b;
    var a = 0;
    
    current_no_up = parseInt(c("#" +elm_container3+ " .uploaded_images ").length, 10);
  
    b = 0;
    if (typeof(plupload) !== "undefined") {
        var d3 = new plupload.Uploader(ajax_var_upload_3.plupload);
        d3.init();
        d3.bind("FilesAdded", function(e, g) {
            if (ajax_var_upload_3.max_images > 0) { 
                if (current_no_up === 0) {
                    b = ajax_var_upload_3.max_images;
                    if (g.length > b) {
                        current_no_up = b
                    } else {
                        current_no_up = g.length
                    }
                } else {

                    if (current_no_up >= ajax_var_upload_3.max_images) {
                        b = -1
                    } else {
                        b = ajax_var_upload_3.max_images - current_no_up;
                        if (g.length > b) {
                            current_no_up = current_no_up + b
                        } else {
                            current_no_up = current_no_up + g.length
                        }
                    }
                }
                if(ajax_var_upload_3.max_images == 1) {
                    b = 1;
                    current_no_up = 0;
                }
              
                if (b > 0) {
                    e.files.slice(0, b);
                    g.slice(0, b);
                    var f = b;
                    while (g.length > b) {
                        e.files.pop();
                        g.pop();
                        a = 1
                    }
                }
                if (a === 1) {
                    c("#" + elm_container3 + ".image_max_warn").remove();
                    c("#" + elm_container3 + " #"+imagelist3).before('<div class="image_max_warn" style="width:100%;float:left;">' + ajax_var_upload_3.warning_max + "</div>")
                }
                
                if (b == -1) {
                    c("#" + elm_container3 + ".image_max_warn").remove();
                    c("#" + elm_container3 + " #"+imagelist3).before('<div class="image_max_warn" style="width:100%;float:left;">' + ajax_var_upload_3.warning_max + "</div>");
                    g = [];
                    e = [];
                    
                    return
                }
            }
            c.each(g, function(j, h) {
                c("#" + elm_container3 + "  #"+elm_uploaded_images3).append('<div id="' + h.id + '">' + h.name + " (" + plupload.formatSize(h.size) + ") <b></b></li>")
            });
            e.refresh();
            d3.start()
        });
        d3.bind("UploadProgress", function(e, f) {
            c("#" + elm_container3 + "  #" + f.id + " b").html(f.percent + "%")
        });
        d3.bind("Error", function(e, f) {
            c("#" + elm_container3 + "  #"+elm_uploaded_images3).append("<div>Error: " + f.code + ", Message: " + f.message + (f.file ? ", File: " + f.file.name : "") + "</li>");
            e.refresh()
        });
        d3.bind("FileUploaded", function(f, i, h) {
            var e = c.parseJSON(h.response);
            c("#" + elm_container3 + "  #image_warn").remove();
            c("#" + elm_container3 + "  #" + i.id).remove();
            if (e.success) {
                c("#" + elm_container3 + "  #preview-image").attr("src", e.html);
                c("#" + elm_container3 + "  #preview-image").attr("data-profileurl", e.html);
                c("#" + elm_container3 + "  #preview-image").attr("data-smallprofileurl", e.attach);
                c("#" + elm_container3 + "  #preview-image").show();

                if (ajax_var_upload_3.max_images == 1) {
                    g = e.attach;
                } else {
                    var g = c("#" + elm_container3 + " #attachid").val();
                    g = g + "," + e.attach;
                }
            

                c("#" + elm_container3 + "  #attachid").val(g);
                if (e.html !== "") {
                    if (ajax_var_upload_3.is_floor === "1") {
                        c("#" + elm_container3 + "  #no_plan_mess").remove();
                        c("#" + elm_container3 + " #"+imagelist3).append('<div class="uploaded_images floor_container" data-imageid="' + e.attach + '"><input type="hidden" name="plan_image_attach[]" value="' + e.attach + '"><input type="hidden" name="plan_image[]" value="' + e.html + '"><img src="' + e.html + '" alt="thumb" /><i class="fa deleter fa-trash-o"></i>' + to_insert_floor + "</div>")
                    } else {
                        c("#" + elm_container3 + " #"+imagelist3).append('<div class="uploaded_images" data-imageid="' + e.attach + '"><img src="' + e.html + '" alt="thumb" /><i class="fa deleter fa-trash-o"></i> </div>')
                    }
                } else {
                    c("#" + elm_container3 + " #"+imagelist3).append('<div class="uploaded_images" data-imageid="' + e.attach + '"><img src="' + ajax_var_upload_3.path + '/img/pdf.png" alt="thumb" /><i class="fa deleter fa-trash-o"></i> </div>')
                }
                if (jQuery().sortable) {
                    c("#" + elm_container3 + " #"+imagelist3).sortable({
                        revert: true,
                        update: function(l, m) {
                            var k, j;
                            k = "";
                            c("#" + elm_container3 + "  #"+imagelist3+" .uploaded_images").each(function() {
                                j = c(this).attr("data-imageid");
                                if (typeof j != "undefined") {
                                    k = k + "," + j
                                }
                            });
                            c("#" + elm_container3 + "  #attachid").val(k)
                        },
                    })
                }
                delete_binder(elm_container3, imagelist3);
                thumb_setter(elm_container3, imagelist3);
                max_image_checker_remove()
            } else {
                if (e.image) {
                    c("#" + elm_container3 + "  #preview-image").hide();
                    c("#" + elm_container3 + " #"+imagelist3).before('<div id="image_warn" style="width:100%;float:left;">' + ajax_var_upload_3.warning + "</div>")
                }
            }
        });
        jQuery("#" + elm_container3 + "  #"+btn_id3).click(function(f) {
            d3.splice();
            d3.refresh()
        });
        c("#" + elm_container3 + "  #aaiu-uploader2").click(function(f) {
            d3.start();
            f.preventDefault()
        });
        c("#" + elm_container3 + "  #aaiu-uploader-floor").click(function(f) {
            f.preventDefault();
            c("#aaiu-uploader").trigger("click")
        });
        d3.splice();
        d3.refresh()
    }
    d3.splice();
    d3.refresh()
});
